package ex07;

public class MaiorNota {
    public static void main(String[] args) {
        String maiorNota, maiorAlunoN;
    Writer w;
    Reader r;

    while (r.read(pathname) != null) {
       alunoTemp = r.read(pathname);
       if (r.read().read() > maiorNota) {
            r.read() = maiorAlunoN;
            r.read() = maiorNota;
       }
    }
    System.out.println("Aluno: " + maiorAlunoN + "\n" + "Nota: " + maiorNota);
    }
}
